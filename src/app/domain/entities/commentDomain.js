// Object comment for domain layer

const commentDomain = (id, idUser, userName, comment) => {
    return {
        id: id,
        idUser: idUser,
        userName, userName,
        comment: comment
    }
}

module.exports = {
    commentDomain
}